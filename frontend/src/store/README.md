# store/

Reserved for global application state (e.g. authentication session).

Do not add a state library here until a concrete need exists. Auth and other global concerns will land in a later feature.
